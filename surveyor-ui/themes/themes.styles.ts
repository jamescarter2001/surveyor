import { createTheme } from "@mui/material";

/**
 * Material UI theme containing the core colour palette of the app.
 */
export const theme = createTheme({
    palette: {
        primary: {
            main: '#024B7E',
            light: '#024B7E',
            dark: '#03357E'
        },
        secondary: {
            main: '#FFFFFF'
        }
    }
})